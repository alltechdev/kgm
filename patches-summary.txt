- Discover tab removed
- All WebView activities and fragments disabled
- Link previews in chat disabled (URLs stay tappable)
- Facebook, Spotify, and Interests sections removed from Profile
- Help Center row and Profile glow-up banner removed from More tab
- Camera (OneCameraActivity) fixed: falls back to internal getFilesDir() when getExternalFilesDir() returns null instead of crashing on /OneCameraClips at root
- Facebook / Google / Microsoft sign-in buttons hidden (don't work on a debug-signed APK anyway) - only email + phone remain
- Copilot stripped: CopilotV2State.isCopilotChatEnabled/isCopilotGroupMentionSettingUIEnabled forced false (cascades to all gated checks), and every Copilot Fragment/Activity (Ask Copilot, consent sheets, quiz/flashcard full-screens, leaderboard, chat selector, settings, group "Message Copilot" row) is killed at onCreateView/onCreate
- Profile pictures / avatars hidden across every screen (GroupMe AvatarView + Fluent AvatarView/AvatarGroupView onAttachedToWindow zeros layout params + walks one parent up and hides containers whose resource name contains "avatar")
- Notification avatars stripped: BaseNotification.setLargeIcon forced null, every Person.Builder.setIcon (Conversation/ConversationMention/SingleMessage) nulled, and conversation shortcut icons (SharingShortcutsManager) nulled so system-conversation notifications no longer pull sender avatars
- Pinned-groups carousel removed from chat list (both addHeaderView call sites)
- Toolbar title changed from "Chat" to "Messages" with letter spacing 0.02f applied to every TextView child of the toolbar
- "Add contact" and "Share profile" icons removed from chat list toolbar (only Search kept)
- Share icon removed from Group Members screen toolbar
- "DMs" filter pill renamed to "People" (text override at ChatFilterAdapter$FilterViewHolder.bind; internal id stays "dms" so the ECS allowlist check still matches)
- "New Chat" screen renamed to "New Message" (both setTitle sites in StartDMFragment)
- "Create Event Group" and "Create Vanishing Group" entries removed from new-chat sheet (filtered from _startItems in StartNewChatItemsAdapter constructor)
- "Create Group" and "Create Announcement Group" entries keep title only - icon ImageButton and description TextView hidden in bind
- Bottom navigation bar collapsed to height 0 (kept visible so FAB's CoordinatorLayout anchor still resolves at the bottom of the screen)
- Settings entry added to a 3-dot overflow in the top-right of the chat list - taps fire mBottomNavigationView.setSelectedItemId(R.id.bottom_more), which routes through the existing OnItemSelectedListener -> loadMoreFragment; back-press then calls setSelectedItemId(bottom_chat) which transitions back to the chat list
- Filter funnel icon added to chat list toolbar next to search; opens a SubMenu with All / Unread / People / Groups - each item invokes ChatFilterAdapter.kgmSelectFilterById which posts the matching Filter object to ConversationViewModel.getChatsFilter() (the same path the pills used). Plain updateSelectedFilter only flipped the adapter's visual selection - the data filter requires the LiveData post. The new toolbar icons (filter + New Message) are tinted with R.color.primary_icon via MenuUtils.tintMenuItemIcons at the end of kgmAddFilterMenu so they read against the toolbar in both light and dark themes (previously they rendered as white-on-white in light mode).
- Filter pills row removed from chat list (both addHeaderView call sites for mFilterContainer)
- "New Message" action icon added to the chat list toolbar (ic_fluent_compose_24_filled); taps call HomeActivity.launchNewChatActivity (same entry point the FAB used)
- Floating-action-button (compose) suppressed: kept in the CoordinatorLayout so fab_menu_anchor's layout_anchor still resolves, but kgmLiftFab zeros its LayoutParams width and height + setVisibility(GONE), and GradientActionButton overrides setVisibility / show() / show(OnVisibilityChangedListener) as no-ops so internal FAB.show() calls (which bypass dynamic dispatch via super.setVisibility) can't re-show it
- Two static helpers added to HomeActivity (kgmCollapseView, kgmLiftFab) so the height/visibility tweaks live in 4-bit register methods - earlier inline edits in onCreate tripped the verifier
- Chat list contact / group names rendered with sans-serif-black + Paint.setFakeBoldText(true) in the ConversationListItemViewHolder constructor (.locals 1 -> 3) so they read as extra heavy without bundling a custom font
- Top bar (toolbar + system status bar) painted #F0EDEF via a new HomeActivity.kgmTintToolbar(Activity, Toolbar). BaseActivity.onCreate calls EdgeToEdge.enable which makes the system status bar transparent, so Window.setStatusBarColor was effectively no-op; the helper clears FLAG_TRANSLUCENT_STATUS, adds FLAG_DRAWS_SYSTEM_BAR_BACKGROUNDS, calls setStatusBarColor, sets the decor view background to #F0EDEF (the actual color that shows through the transparent status bar), and ORs SYSTEM_UI_FLAG_LIGHT_STATUS_BAR into systemUiVisibility so the status bar icons render dark on the light bg.
- Conversation list fragment root explicitly painted white in onCreateView right after inflate so the decor view's #F0EDEF only shows through the transparent status bar (and not the chat list body)
- Lens "Selected file format is not supported" gate bypassed: FileUtils.isValidImageFile in com.microsoft.office.lenssdk.utils now returns true for any non-null URI so HEIC and other formats the original allowlist missed (jpeg/jpg/png/bmp/gif/webp/heif) are accepted; Lens decodes them directly instead of popping the unsupported-format dialog.
- OneCamera in-chat "photos" import button shows both images and videos: CaptureFragment.launchGetContentActivityResultLauncher now ignores its ImportConfig argument and fires a manual ACTION_GET_CONTENT intent with type=*/* and EXTRA_MIME_TYPES=["image/*","video/*"] + EXTRA_ALLOW_MULTIPLE (Android 13+ shows the system photo picker with mixed media; older Android shows the chooser with Photos+SAF). A new CaptureFragment.onActivityResult override (request code 0x6b47) extracts URIs from clipData/getData and calls captureViewModel.onContentReturnedForImportConfig - the same internal callback the original launcher results funneled into - so the preview/confirm/return pipeline is unchanged. Original library default ImportConfig of MultipleVideoFiles in ImportMediaButton was also flipped to MultiplePhotoFiles as a safety net, though the bypass makes it moot.
- Calendar + Events stripped app-wide: every event/calendar Activity (EventCreateUpdateActivity, EventDetailActivity, UserCalendarActivity) calls finish() immediately after super.onCreate so any intent that targets them is a no-op. Every event/calendar Fragment with an onCreateView (AttachEventFragment, EventCreateUpdateFragment, EventDetailsFragment, EventsListFragment, JoinEventSuccessFragment, RecurringEventBottomSheetFragment, UserCalendarFragment) has the method rewritten to return null. Calendar tile in MoreTab is skipped (forced v0=0 before the if-eqz so the add block never runs). Event tab dropped from ChatAttachmentPagerAdapter.getPagesDm and getPagesGroup. JoinEventInlineContent.bindView hides the inline card root + zeros its LayoutParams.height so received event cards in chat collapse. TopLevelEventGroupBadgeView (the dated badge that decorates group avatars / list rows) has setVisibility(I) clamped to GONE and onAttachedToWindow zeros its LayoutParams.width and height so no list row reserves space for it. ChatFragment.createContextMenu forces v1=0 right before setVisible on menu_share_event / menu_clone_event so the long-press menu items are hidden. HighlightsBaseFragment.onCreateContextMenu calls menu.removeItem for both ids at method entry.
- Copilot settings entry killed: GlobalPreferences.canShowCopilot(Context) now returns false unconditionally so every settings-fragment branch that gates the Copilot row, the row's on-click handler, and any other call site sees Copilot disabled - the row disappears from settings. Plus GlobalPreferences.<onCreate> (the path that actually builds the Settings PreferenceScreen) gates the SHOW_COPILOT_ACTION row on CopilotV2.isEnabled(); forcing v0=0 right after the move-result makes the if-nez always fall through to removePreferenceSafe(screen, copilotPref) so the row is stripped from the screen regardless of the ECS flag.
- Dark mode passes through unchanged: HomeActivity.kgmTintToolbar bails at the top when Configuration.uiMode & UI_MODE_NIGHT_MASK == UI_MODE_NIGHT_YES (0x20), so the #F0EDEF toolbar paint, status bar color, decor-view paint, and LIGHT_STATUS_BAR system UI flag are all skipped in dark mode. ConversationListFragment.onCreateView chat-list root paint to white is gated behind the same night-mode check.
- Chat attachment tab strip removed: ChatAttachmentFragment.onViewCreated now calls a static helper kgmCollapseTabs(View) on the IconTabLayout immediately after findViewById. The helper sets the tab layout to GONE then walks up its parent chain, zeroing LayoutParams.height at every level whose ViewGroup has fewer than 3 children (i.e., thin wrappers that only hold the tab strip + a divider) so the empty band above the gallery disappears entirely - both the pills row and any space it reserved.
- ShareInterstitialActivity trimmed: the cover_image / shareStickerImageView is hidden + its LayoutParams.height zeroed right after findViewById so the username/title is the only thing above the options list. The share medium array is also overwritten right after viewModel.getFilteredShareMediumList with a freshly-built 2-entry [COPY_LINK, SYSTEM_SHARE] array, so ShareOptionsAdapter only shows Copy Link and Share Via (Instagram Stories / Snapchat / WhatsApp / QR Code all dropped).
